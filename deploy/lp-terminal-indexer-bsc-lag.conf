[Service]
# The reviewed Pancake V3 subgraph intentionally trails the BSC head.  The
# indexer still pins and verifies that immutable snapshot, then catches the
# remaining factory events up through RPC before reporting ready.
Environment=INDEXER_PANCAKE_V3_SUBGRAPH_MAX_LAG_BLOCKS=6000000
